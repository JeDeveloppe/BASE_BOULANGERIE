<form method="post" action="{{ path('horaires_eboutique_delete', {'id': horaires_eboutique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaires_eboutique.id) }}">
    <button class="btn">Delete</button>
</form>
