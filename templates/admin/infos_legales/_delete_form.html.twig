<form method="post" action="{{ path('admin_infos_legales_delete', {'id': infos_legale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_legale.id) }}">
    <button class="btn">Delete</button>
</form>
