{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="col-12 text-center alertDiv bg-{{ label }} alert-dismissible fade show p-2" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}